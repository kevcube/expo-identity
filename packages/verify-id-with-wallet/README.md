# verify-id-with-wallet

This is a library to decrypt the data received from Apple's "VerifyIdentityWithWallet" API.

This data is delivered from the OS to your application, and your application sends to your server for decryption.
